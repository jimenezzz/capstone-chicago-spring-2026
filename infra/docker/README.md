Use the root-level `docker-compose.yml` for local orchestration.

Examples:
- `docker compose --profile core up`
- `docker compose up db`
- `docker compose --profile ui up web`
- `docker compose --profile pipelines run --rm pipelines`
